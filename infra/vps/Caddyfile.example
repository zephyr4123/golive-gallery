# Caddyfile —— VPS 上唯一的常驻服务:一个 Caddy 伺服全部静态站
# 每站一个 site block,root 指向 current 符号链接(发布 = 切链接,回滚 = 切回去)
# HTTPS 证书由 Caddy 内置 ACME 自动签发续期(per-host HTTP-01),零配置零 cron

team.<BASE_DOMAIN> {
	root * /srv/sites/team/current
	file_server
	encode gzip
}

# 加新站 = 复制一个 block 改两处;站点多到上百再考虑通配符证书(DNS-01)
